import { RaidBossAttackResult } from './raid-boss-attack-result';

describe('RaidBossAttackResult', () => {
  it('should create an instance', () => {
    expect(new RaidBossAttackResult(0,0)).toBeTruthy();
  });
});
