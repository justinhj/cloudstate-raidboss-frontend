export class RaidBossAttackResult {
    damage: number;
    health: number;

    constructor(
        damage: number,
        health: number) {
            this.damage = damage;
            this.health = health;
        }
}
